# lists = lists are containers to store a set of values of any datatypes
friends = ["Apple","Akash",3,69,88.33,False]

print(friends[0])

friends[0]="Grapes" # Unlike strings, lists are mutable
print(friends[0])

print(friends[1:4])