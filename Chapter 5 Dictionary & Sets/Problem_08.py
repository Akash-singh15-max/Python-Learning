# Can you change the values inside a list which is contained in set s
# s = {8,7,12,"Harry",[1,2]}

# No, we can't change because
# 1. we can't include a list in a set
# 2. If we can include a list in a set then we won't do it by indexing
